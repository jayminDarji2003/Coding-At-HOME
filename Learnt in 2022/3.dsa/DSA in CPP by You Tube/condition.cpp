// All about conditions in cpp
#include <iostream>
using namespace std;

int main()
{

    // Simple if else condition
    // Program :- find max from two no
    /*
    int a,b;
    cout<<"Enter a and b : ";
    cin>>a>>b;
    if (a>b)
    {
        cout<<"a is max";
    }
    else{
        cout<<"b is max";
    }
    */

    // If else ladder condition
    // Program :- Find number is positive,negative or zero.
    /*
    int num;
    cout << "Enter the value of num : ";
    cin >> num;
    if (num > 0)
    {
        cout << "Positive number";
    }
    else if (num < 0)
    {
        cout << "Negative number";
    }
    else
    {
        cout << "Number is zero";
    }
    */

    // Program to find Uppercese or lowercase
    /*
    char ch;
    cout << "Enter any character : ";
    cin >> ch;
    int num=ch;
    if (ch >= 'a' && ch <= 'z')
    {
        cout << "Number is lowercase" << endl;
        cout<<"Ascci value of ch is : "<<num<<endl;
    }
    else
    {
        cout << "Number is uppercase" << endl;
        cout<<"Ascci value of ch is : "<<num<<endl;
    }
    */

    return 0;
}