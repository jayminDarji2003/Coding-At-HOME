// "this" pointer
/*
     Defnition:- This pointer is a special variable which is used to store the value in the same variable.
        ----> If we have variable
                    int age;
                    age=76;
                And if we want to store in that variable
                    this->age=77;   //this syntax is used.
*/
#include<iostream>
using namespace std;

int main(){
    
    return 0;
}